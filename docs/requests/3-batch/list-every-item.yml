info:
  name: List every item
  type: http
  seq: 3
http:
  method: GET
  url: '{{baseUrl}}/batches/{{batchId}}/items'
  auth: inherit
runtime:
  assertions:
  - expression: res.status
    operator: eq
    value: '200'
