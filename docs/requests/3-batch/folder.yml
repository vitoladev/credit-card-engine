info:
  name: 3. Batch
  type: folder
  seq: 4
docs: 'POST /evaluations/batch stores the customers as QUEUED items and answers 202. The worker decides
  them a moment later. Run "Submit a batch" first: it saves batchId and itemId. Wait a few seconds before
  listing.'
