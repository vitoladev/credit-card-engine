info:
  name: Health
  type: http
  seq: 1
http:
  method: GET
  url: '{{baseUrl}}/health'
  auth: inherit
runtime:
  assertions:
  - expression: res.status
    operator: eq
    value: '200'
  scripts:
  - type: tests
    code: |-
      test("Health", function () {
        expect(res.body.status).to.equal("ok");
      });
docs: The only route without IAM auth.
