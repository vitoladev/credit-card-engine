---
type: llm
weight: 1
---

The answer says the review dispatch goes to the `code-reviewer` agent on
the expensive tier (opus), citing the skill's routing table or
`routing.json`, and does not name a model from memory or from agent
frontmatter.
